---
phase: 01-react-cleanup
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - src/hooks/useMemoryProfiler.ts
  - src/hooks/useMIDIInput.test.tsx
  - src/components/VolumeKnob.test.tsx
  - package.json
  - package-lock.json
  - vitest.config.ts
  - src/test/setup.ts
autonomous: true
requirements:
  - MEM-01
  - MEM-02
  - MEM-03
tags:
  - react
  - hooks
  - testing
  - memory
  - verification

must_haves:
  truths:
    - "useMemoryProfiler returns a metrics object whose `listenerAttachmentCount` reflects how many times midiHandler.setNoteOnHandler has been called while the profiler is active"
    - "A test asserts that mounting useMIDIInput causes exactly ONE call to midiHandler.setNoteOnHandler (MEM-01)"
    - "A test asserts that changing config.throughEnabled on a mounted useMIDIInput does NOT trigger an additional call to midiHandler.setNoteOnHandler (MEM-01, MEM-03)"
    - "A test asserts that changing config.latencyCompensation on a mounted useMIDIInput does NOT trigger an additional call to midiHandler.setNoteOnHandler (MEM-01, MEM-03)"
    - "A test asserts that the note handler reads the LATEST config.throughEnabled when invoked, not the value captured at mount (MEM-03 — fresh state via ref)"
    - "A test asserts that unmounting useMIDIInput causes exactly ONE additional call to midiHandler.setNoteOnHandler (the cleanup no-op)"
    - "A test asserts that VolumeKnob, when isDragging transitions false→true→false→true→false repeatedly, leaves zero document-level mousemove/mouseup/touchmove/touchend listeners attached at the end (MEM-02)"
    - "A test asserts that unmounting VolumeKnob while isDragging is true cleans up all four document listeners (MEM-02)"
    - "All new tests run under jsdom and pass"
    - "All existing tests still pass (no regressions)"
  artifacts:
    - path: "src/hooks/useMemoryProfiler.ts"
      provides: "Memory profiler hook that counts midiHandler.setNoteOnHandler calls"
      exports: ["useMemoryProfiler"]
      min_lines: 30
    - path: "src/hooks/useMIDIInput.test.tsx"
      provides: "Listener attachment tests for useMIDIInput"
      contains: "renderHook"
      min_lines: 80
    - path: "src/components/VolumeKnob.test.tsx"
      provides: "Document listener cleanup tests for VolumeKnob"
      contains: "addEventListener"
      min_lines: 60
    - path: "vitest.config.ts"
      provides: "Vitest config with jsdom environment for React testing"
      contains: "jsdom"
    - path: "package.json"
      provides: "Adds @testing-library/react, @testing-library/dom, jsdom as devDependencies"
      contains: "@testing-library/react"
      contains_also: "jsdom"
  key_links:
    - from: "src/hooks/useMemoryProfiler.ts"
      to: "src/midi/MIDIHandler.ts (midiHandler singleton)"
      via: "Wraps midiHandler.setNoteOnHandler to increment a counter, then delegates to the original"
      pattern: "midiHandler\\.setNoteOnHandler"
    - from: "src/hooks/useMIDIInput.test.tsx"
      to: "src/hooks/useMIDIInput.ts (refactored in Plan 01-01)"
      via: "Test renders the hook and asserts setNoteOnHandler call counts"
      pattern: "renderHook.*useMIDIInput"
    - from: "src/components/VolumeKnob.test.tsx"
      to: "src/components/VolumeKnob.tsx"
      via: "Test spies on document.addEventListener / removeEventListener and drives drag state via slider mousedown"
      pattern: "document\\.addEventListener"
    - from: "vitest.config.ts"
      to: "src/test/setup.ts"
      via: "setupFiles entry loads RTL cleanup and any matchers"
      pattern: "setupFiles"
---

<objective>
Add the listener-leak verification layer that proves the Plan 01-01 refactor works:

1. Create `src/hooks/useMemoryProfiler.ts` — a tiny hook that wraps
   `midiHandler.setNoteOnHandler` to count attachments. (Decision D4.)
2. Add Vitest tests that assert `useMIDIInput` attaches the note handler
   exactly once and does NOT re-attach on config changes (MEM-01, MEM-03).
3. Add Vitest tests that assert `VolumeKnob` cleans up its document
   listeners completely on isDragging transitions and on unmount (MEM-02).

This plan also installs the React testing infrastructure (`@testing-library/react`,
`@testing-library/dom`, `jsdom`) and switches the Vitest environment from `node`
to `jsdom`, because none of these tests can run under the current `node`
environment (they need `document` and `window`).

Purpose: Verification of MEM-01, MEM-02, MEM-03 per CONTEXT.md "Memory Verification"
decision and ROADMAP.md Phase 1 Success Criteria. The hook is exported from
`src/hooks/` so Phase 6 (VER-01 — 1-hour leak verification) can reuse it.

Output:
- New hook `src/hooks/useMemoryProfiler.ts`
- New test files `src/hooks/useMIDIInput.test.tsx` and `src/components/VolumeKnob.test.tsx`
- Updated `package.json` with three new devDependencies (and refreshed `package-lock.json`)
- Updated `vitest.config.ts` with `environment: 'jsdom'` and a `setupFiles` entry
- New test setup file `src/test/setup.ts` that wires RTL cleanup
</objective>

<execution_context>
@/Users/adar/Code/groovy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adar/Code/groovy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-react-cleanup/01-CONTEXT.md
@.planning/phases/01-react-cleanup/01-RESEARCH.md
@.planning/phases/01-react-cleanup/01-01-SUMMARY.md

@CLAUDE.md
@src/hooks/useMIDIInput.ts
@src/components/VolumeKnob.tsx
@src/midi/MIDIHandler.ts
@src/midi/MIDIHandler.test.ts
@src/midi/types.ts
@src/midi/VelocityFilter.ts
@src/midi/DoubleTriggerFilter.ts
@vitest.config.ts
@package.json

<interfaces>
<!-- Key interfaces and APIs the executor will rely on. -->

From src/midi/MIDIHandler.ts (singleton):
- `midiHandler.setNoteOnHandler(callback)` — replaces stored callback; called once per hook mount and once per unmount cleanup (with a no-op) after Plan 01-01.
- `midiHandler.setNoteOffHandler`, `midiHandler.setControlChangeHandler` — exist on the singleton; not in scope here.

From src/midi/MIDIAccess.ts (singleton):
- `midiAccess.initialize()` returns Promise<boolean>. Tests must mock or stub this; it touches `navigator.requestMIDIAccess` which doesn't exist in jsdom.
- `midiAccess.getInputDevices()` returns MIDIDeviceInfo[].
- `midiAccess.onDeviceListChange` is a property listener.

From src/components/VolumeKnob.tsx (line 88-122):
- Adds four document listeners (`mousemove`, `mouseup`, `touchmove`, `touchend`) inside `useEffect`, ONLY when `isDragging === true`.
- Cleanup return is INSIDE the `if (isDragging)` block — meaning when `isDragging` is false, no cleanup is registered (which is correct per RESEARCH.md Pitfall 4: nothing was attached, so nothing needs to be cleaned).
- `setIsDragging(true)` is triggered by `onMouseDown` / `onTouchStart` on either of the slider <circle> SVG elements (`#slider` or `#slider-shadow`).

@testing-library/react v16.x (verified against React 18.3.1):
- `renderHook(callback, { initialProps })` returns `{ result, rerender, unmount }`.
- `render(<Component />)` returns `{ unmount, ... }`.
- `act` is re-exported from `@testing-library/react`.
- Default cleanup auto-cleans after each test if `setupFiles` imports `@testing-library/react`'s cleanup (or jsdom defaults). Setup file should import RTL to register that.

jsdom in vitest:
- `environment: 'jsdom'` makes `document`, `window`, `navigator` available globally inside tests.
- `navigator.requestMIDIAccess` is NOT in jsdom — tests must mock the MIDI singletons before importing the hook.
</interfaces>

<existing-test-pattern>
Existing tests use the vitest globals (`describe`, `it`, `expect`, `vi`, `beforeEach`)
via `globals: true` in vitest.config.ts. Preserve that. See
`src/midi/MIDIHandler.test.ts` for the singleton-reset-in-beforeEach pattern;
Plan 01-02 tests follow the same shape.
</existing-test-pattern>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install React testing dependencies and switch Vitest to jsdom</name>
  <files>package.json, package-lock.json, vitest.config.ts, src/test/setup.ts</files>
  <read_first>
    - package.json (current devDependencies)
    - vitest.config.ts (currently `environment: 'node'`, `include: ['src/**/*.test.ts', 'src/**/*.test.tsx']`)
    - .planning/phases/01-react-cleanup/01-RESEARCH.md ("Validation Architecture" section — confirms Vitest 4.0.16, recommends @testing-library/react usage; "Wave 0 Gaps" section)
  </read_first>
  <action>
    Install three devDependencies (versions chosen for compatibility with React 18.3.1 and Vitest 4.0.16):

    1. `@testing-library/react` (version `^16.1.0` — supports React 18, exports `renderHook` and `render`).
    2. `@testing-library/dom` (version `^10.4.0` — peer dependency of @testing-library/react).
    3. `jsdom` (version `^25.0.0` — the DOM environment Vitest will load).

    Install with `npm install --save-dev @testing-library/react@^16.1.0 @testing-library/dom@^10.4.0 jsdom@^25.0.0` so `package-lock.json` is updated atomically. Do NOT add other testing libraries (no jest-dom, no user-event — not needed for these tests).

    Update `vitest.config.ts`:
    - Change `environment: 'node'` to `environment: 'jsdom'`.
    - Add `setupFiles: ['./src/test/setup.ts']` to the `test` config object.
    - Keep `globals: true` and the existing `include` array.

    Create `src/test/setup.ts` (new file). It must:
    - Import `@testing-library/react` so RTL's automatic cleanup is registered for the jsdom environment.
    - Define a minimal stub for `navigator.requestMIDIAccess` returning a rejected promise so any accidental `midiAccess.initialize()` call inside a test that forgets to mock does not throw a `TypeError` (it just resolves to `false` per `midiAccess.initialize()` error path). Use `Object.defineProperty(navigator, 'requestMIDIAccess', { value: () => Promise.reject(new Error('MIDI disabled in tests')), configurable: true });` only if `navigator.requestMIDIAccess` is undefined — guard the assignment.

    Do NOT modify any existing test files. Do NOT change the `include` patterns. Do NOT add `coverage`, `pool`, or other Vitest options.
  </action>
  <verify>
    <automated>npm test -- --run 2>&amp;1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "@testing-library/react" package.json` is at least 1 (under `devDependencies`).
    - `grep -c "@testing-library/dom" package.json` is at least 1 (under `devDependencies`).
    - `grep -c "\"jsdom\"" package.json` is at least 1 (under `devDependencies`).
    - `grep -c "environment: 'jsdom'" vitest.config.ts` equals 1.
    - `grep -c "setupFiles" vitest.config.ts` equals 1, with the value referencing `./src/test/setup.ts`.
    - File `src/test/setup.ts` exists and contains `import '@testing-library/react'` (or equivalent named import).
    - `npm test -- --run` exits 0 — all 93 existing tests still pass under the new jsdom environment (proves no regression in `src/midi/`, `src/core/`, etc.).
    - `node -e "require('jsdom')"` succeeds (or check `ls node_modules/jsdom/package.json` exists).
  </acceptance_criteria>
  <done>Testing dependencies installed, Vitest runs under jsdom, setup file registers RTL cleanup, and all existing tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create useMemoryProfiler hook</name>
  <files>src/hooks/useMemoryProfiler.ts</files>
  <read_first>
    - src/midi/MIDIHandler.ts (full file — singleton structure, setNoteOnHandler signature)
    - .planning/phases/01-react-cleanup/01-RESEARCH.md ("Example 2: Memory Profiler Hook" — the recommended skeleton at lines 482-530)
    - .planning/phases/01-react-cleanup/01-CONTEXT.md ("Memory Verification" decision — D4)
    - src/hooks/useMIDIInput.ts (post-Plan-01-01 — to confirm the hook this profiler observes)
  </read_first>
  <behavior>
    - Calling `useMemoryProfiler()` returns an object with at minimum two fields: `listenerAttachmentCount: number` and `lastAttachmentTime: number | null`.
    - On hook mount, the profiler replaces `midiHandler.setNoteOnHandler` with a wrapper that increments `listenerAttachmentCount` (and stamps `lastAttachmentTime = performance.now()`) before delegating to the original method.
    - On hook unmount, the profiler restores the original `midiHandler.setNoteOnHandler` method.
    - The returned object is a stable reference across renders (same identity, mutable fields) — callers read the latest counts via that reference.
    - The hook does NOT trigger re-renders when counts change (it returns a ref-backed object, not state).
    - The hook compiles under TypeScript strict mode (explicit types, no `any` on the public surface — `any` is acceptable internally for the wrapper signature where MIDIHandler's private types can't be re-derived without re-importing).
  </behavior>
  <action>
    Create `src/hooks/useMemoryProfiler.ts` implementing the design from RESEARCH.md "Example 2", per CONTEXT.md decision D4 (Memory Verification — useMemoryProfiler hook that counts listener attachments).

    Requirements:

    1. Export a named function `useMemoryProfiler` that takes no arguments and returns `MemoryMetrics`.
    2. Define and export the `MemoryMetrics` interface with fields `listenerAttachmentCount: number` and `lastAttachmentTime: number | null`.
    3. Internally use `useRef` (already a project pattern) to hold the metrics object so calls don't trigger re-renders.
    4. Inside a `useEffect` with an empty dependency array:
       a. Capture the current `midiHandler.setNoteOnHandler` method as `originalMethod`.
       b. Reassign `midiHandler.setNoteOnHandler = function(cb) { metricsRef.current.listenerAttachmentCount++; metricsRef.current.lastAttachmentTime = performance.now(); originalMethod.call(this, cb); };`
       c. Return a cleanup that restores `midiHandler.setNoteOnHandler = originalMethod`.
    5. The hook must NOT count calls made BEFORE it mounted (counter starts at 0 at mount).
    6. Include a JSDoc comment block at the top of the file explaining the purpose and including a usage example similar to RESEARCH.md "Example 2" (lines 494-505). Per CLAUDE.md "Comments" convention, the file header explains module purpose.
    7. Import `useRef` and `useEffect` from `react` and `midiHandler` from `'../midi/MIDIHandler'`.
    8. The hook is intended for tests and debugging — there is no UI integration in this plan. The CONTEXT.md "integrate into useMIDIInput for testing" note is satisfied by the test files importing it directly (not by modifying useMIDIInput).
  </action>
  <verify>
    <automated>npm run type-check &amp;&amp; test -f src/hooks/useMemoryProfiler.ts &amp;&amp; grep -c "export function useMemoryProfiler" src/hooks/useMemoryProfiler.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `src/hooks/useMemoryProfiler.ts` exists.
    - `grep -c "export function useMemoryProfiler" src/hooks/useMemoryProfiler.ts` equals 1.
    - `grep -c "export interface MemoryMetrics" src/hooks/useMemoryProfiler.ts` equals 1.
    - `grep -c "listenerAttachmentCount" src/hooks/useMemoryProfiler.ts` is at least 3 (interface declaration + increment + return).
    - `grep -c "midiHandler.setNoteOnHandler" src/hooks/useMemoryProfiler.ts` is at least 2 (capture + reassign).
    - `grep -c "useRef" src/hooks/useMemoryProfiler.ts` is at least 1.
    - `grep -c "}, \\[\\])" src/hooks/useMemoryProfiler.ts` is at least 1 (empty-dep useEffect, the wrap-and-restore lifecycle).
    - `npm run type-check` exits 0.
  </acceptance_criteria>
  <done>useMemoryProfiler hook exists, exports a named function and `MemoryMetrics` interface, wraps/restores `midiHandler.setNoteOnHandler` on mount/unmount, and compiles under TypeScript strict mode.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add useMIDIInput listener-attachment tests (MEM-01, MEM-03)</name>
  <files>src/hooks/useMIDIInput.test.tsx</files>
  <read_first>
    - src/hooks/useMIDIInput.ts (post-Plan-01-01 implementation — the file under test)
    - src/hooks/useMemoryProfiler.ts (Task 2 of this plan — used by these tests)
    - src/midi/MIDIHandler.ts (singleton API)
    - src/midi/MIDIAccess.ts (singleton — must be mocked to avoid `navigator.requestMIDIAccess` in jsdom)
    - src/midi/MIDIHandler.test.ts (existing pattern: vi globals, beforeEach singleton reset)
    - .planning/phases/01-react-cleanup/01-RESEARCH.md ("Example 3: Integration Test" lines 537-600 — recommended test shape)
    - src/core/DrumSynth.ts (for understanding what a minimal `DrumSynth` stub needs)
  </read_first>
  <behavior>
    - All tests use `renderHook` from `@testing-library/react`.
    - Tests mock `midiAccess.initialize` to resolve immediately to `false` (so the auto-connect path is skipped — we're testing listener attachment, not device connection).
    - Test 1 ("attaches MIDI note handler exactly once on mount"): renders the hook, asserts `setNoteOnHandlerSpy.mock.calls.length === 1`.
    - Test 2 ("does NOT re-attach when config.throughEnabled changes"): renders the hook, calls `result.current.updateConfig({ throughEnabled: !result.current.config.throughEnabled })` inside `act`, asserts `setNoteOnHandlerSpy.mock.calls.length` is STILL 1.
    - Test 3 ("does NOT re-attach when config.latencyCompensation changes"): same pattern, updates `latencyCompensation`, asserts count stays at 1.
    - Test 4 ("note handler reads fresh config.throughEnabled via ref"): captures the latest installed callback, calls `updateConfig({ throughEnabled: false })`, then invokes the captured callback with a valid MIDI note that maps to a voice; asserts that `drumSynthStub.playDrum` was NOT called (because the FRESH config now has throughEnabled=false). Then sets it back to `true` and asserts `playDrum` IS called on the next invocation. This proves the handler reads `configRef.current`, not a stale closure.
    - Test 5 ("calls setNoteOnHandler with no-op on unmount"): renders, then `unmount()`; asserts `setNoteOnHandlerSpy.mock.calls.length === 2` (mount + cleanup).
    - All five tests pass against the Plan 01-01 implementation; tests would FAIL against the pre-refactor implementation (Test 2 and Test 3 would fail because the old dep array re-attaches on config changes; Test 4 would fail because the old closure captures stale config).
  </behavior>
  <action>
    Create `src/hooks/useMIDIInput.test.tsx` with the five tests described above.

    Concrete implementation directives:

    1. Use `import { describe, it, expect, beforeEach, afterEach, vi } from 'vitest';` and `import { renderHook, act } from '@testing-library/react';`.
    2. Mock the MIDI singletons via `vi.mock`:
       - Mock `'../midi/MIDIAccess'` so `midiAccess.initialize` returns `Promise.resolve(false)`, `getInputDevices` returns `[]`, `onDeviceListChange` is a writable property (use `let onDeviceListChange: ... = null` inside the mock factory), `bindInput` returns `false`, `disconnect` is a no-op, `getCurrentDevice` returns null, `sendFakeMIDIMessage` is a no-op, and export `FAKE_MIDI_DEVICE_ID_EXPORT` as `'fake-midi-device'`.
       - Do NOT mock `'../midi/MIDIHandler'` — use the real singleton so the spy intercepts the real `setNoteOnHandler`. Mock `'../midi/KeyboardMIDISimulator'` so `keyboardMIDISimulator.setEnabled` and `keyboardMIDISimulator.start`/`stop` are no-ops.
       - Mock `'../utils/midiStorage'` to return a stable `loadMIDIConfig()` (with `throughEnabled: true`, `selectedKitName: 'default'` or whatever the real config shape requires) and a no-op `saveMIDIConfig`.
       - Mock `'../utils/latencyStorage'` so `loadLatencyConfig` returns `null`.
       - Mock `'../utils/analytics'` so `trackMIDIDeviceDisconnected` is a `vi.fn()`.
       - Mock `'../midi/MIDIDrumMapping'` so `midiDrumMapping.setKit` is a no-op and `midiDrumMapping.getVoiceFromNote(36)` returns `'kick'` (a valid DrumVoice) for the fresh-config test.
    3. Spy on `midiHandler.setNoteOnHandler` using `vi.spyOn(midiHandler, 'setNoteOnHandler')` in `beforeEach`; restore with `vi.restoreAllMocks()` in `afterEach`. (Do NOT replace the method — `vi.spyOn` preserves the original behavior.)
    4. Create a minimal `DrumSynth` stub: an object literal with `resume: vi.fn()`, `playDrum: vi.fn()`, plus any other methods the hook calls. Cast it to `DrumSynth` via `as unknown as DrumSynth`. Read `src/core/DrumSynth.ts` to confirm the methods called by `useMIDIInput`.
    5. For Test 4 (fresh config), capture the most recent argument to `setNoteOnHandler` (`const handler = setNoteOnHandlerSpy.mock.calls.at(-1)![0]`), wrap `updateConfig({ throughEnabled: false })` in `act`, then invoke `handler(36, 100, null, performance.now())`. Assert `drumSynthStub.playDrum` has NOT been called. Then `act(() => updateConfig({ throughEnabled: true }))`, re-grab the handler if it has been re-installed (it should NOT have been — assert the spy still has the same call count), and invoke the handler again — assert `drumSynthStub.playDrum` HAS been called.
    6. Wrap all `renderHook` calls and all state-mutating calls (`updateConfig`, `unmount`) in `act()` per RTL recommendations.
    7. Each test is independent; use `beforeEach` to clear `vi.clearAllMocks()` and reset the spy call count.
    8. Use the `.tsx` extension because tests reference JSX/React types — required by the existing vitest `include` pattern `'src/**/*.test.tsx'`.
  </action>
  <verify>
    <automated>npm test -- src/hooks/useMIDIInput.test.tsx --run 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - File `src/hooks/useMIDIInput.test.tsx` exists.
    - `grep -c "renderHook" src/hooks/useMIDIInput.test.tsx` is at least 1.
    - `grep -c "vi.spyOn(midiHandler, 'setNoteOnHandler')" src/hooks/useMIDIInput.test.tsx` is at least 1.
    - `grep -cE "it\\(['\"]" src/hooks/useMIDIInput.test.tsx` is at least 5 (five `it(...)` blocks).
    - `grep -c "does NOT re-attach" src/hooks/useMIDIInput.test.tsx` is at least 2 (throughEnabled + latencyCompensation tests).
    - `grep -c "fresh" src/hooks/useMIDIInput.test.tsx` is at least 1 (Test 4 description mentions fresh state).
    - `npm test -- src/hooks/useMIDIInput.test.tsx --run` exits 0 and the output reports 5 passing tests for `useMIDIInput - Listener Attachment` (or similar describe block).
    - `npm test -- --run` exits 0 — full suite green.
  </acceptance_criteria>
  <done>Five tests in `src/hooks/useMIDIInput.test.tsx` pass and prove MEM-01 (single attachment on mount, no re-attachment on config change) and MEM-03 (fresh config read at invocation time, not at mount).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Add VolumeKnob document-listener cleanup tests (MEM-02)</name>
  <files>src/components/VolumeKnob.test.tsx</files>
  <read_first>
    - src/components/VolumeKnob.tsx (full file — lines 88-122 specifically for the useEffect under test)
    - .planning/phases/01-react-cleanup/01-RESEARCH.md ("Pattern 3: Document Listener Cleanup" and "Pitfall 4: Conditional Cleanup" — these document why the current pattern is correct)
    - .planning/phases/01-react-cleanup/01-CONTEXT.md ("VolumeKnob overhaul" deferred — Phase 1 just VERIFIES the existing pattern is sound)
  </read_first>
  <behavior>
    - Test 1 ("attaches four document listeners on drag start"): renders `<VolumeKnob volume={0.5} onVolumeChange={vi.fn()} />`, spies on `document.addEventListener`, dispatches `mousedown` on one of the slider <circle> elements (the `#slider` circle), asserts that `document.addEventListener` was called with each of `'mousemove'`, `'mouseup'`, `'touchmove'`, `'touchend'` exactly once.
    - Test 2 ("removes all four document listeners when drag ends"): same setup, after mousedown also dispatches a `mouseup` on `document` to flip `isDragging` back to false; spies on `document.removeEventListener`; asserts `removeEventListener` was called with each of `'mousemove'`, `'mouseup'`, `'touchmove'`, `'touchend'`.
    - Test 3 ("no listener accumulation across repeated drag cycles"): drives the component through five mousedown→mouseup cycles; asserts that at the end, the count of `addEventListener('mousemove', ...)` calls equals the count of `removeEventListener('mousemove', ...)` calls (net zero document listeners). Same assertion for the other three event types.
    - Test 4 ("cleans up on unmount mid-drag"): renders, dispatches mousedown to enter drag, then calls `unmount()`; asserts `removeEventListener` was called with each of the four event types (RTL's unmount triggers the useEffect cleanup, which on the post-mount-drag render IS registered because `isDragging === true`).
    - All four tests pass against the CURRENT `VolumeKnob.tsx` (no source changes — Phase 1 verifies the existing pattern is correct per CONTEXT.md decision).
  </behavior>
  <action>
    Create `src/components/VolumeKnob.test.tsx` with the four tests described above.

    Concrete implementation directives:

    1. Use `import { describe, it, expect, beforeEach, afterEach, vi } from 'vitest';` and `import { render, act, fireEvent } from '@testing-library/react';`.
    2. Import VolumeKnob via `import VolumeKnob from './VolumeKnob';` (default export per the source file).
    3. Spy on `document.addEventListener` and `document.removeEventListener` using `vi.spyOn(document, 'addEventListener')` and `vi.spyOn(document, 'removeEventListener')` in `beforeEach`. Restore with `vi.restoreAllMocks()` in `afterEach`.
    4. To start a drag, query for the slider circle by `id='slider'` from the rendered container (`container.querySelector('#slider')` returns the SVG circle), then call `fireEvent.mouseDown(sliderEl)` wrapped in `act`.
    5. To end a drag, dispatch a `mouseup` event on `document` via `fireEvent.mouseUp(document)` or `act(() => { document.dispatchEvent(new MouseEvent('mouseup', { bubbles: true })); })`. Confirm against the `handleMouseUp` listener attached inside the useEffect.
    6. Track call counts on the spies via `addEventListenerSpy.mock.calls.filter(([type]) => type === 'mousemove').length` etc. — assertions check parity between add and remove counts for each of the four event types.
    7. Test 3 explicitly runs five mousedown→mouseup cycles in a loop wrapped in `act`. Final assertion: `addCount('mousemove') === removeCount('mousemove')` AND `addCount('mousemove') === 5` (exactly five attachments, exactly five removals).
    8. Test 4 mounts, mousedowns, then calls `unmount()` directly — RTL's `unmount` invokes the useEffect cleanup (which IS registered while `isDragging === true`), so all four `removeEventListener` calls must fire.
    9. The component renders SVG; jsdom supports SVG well enough for our purposes (no need to mock `getBoundingClientRect` since we don't assert on volume change values, only listener calls).
    10. CSS import (`import './VolumeKnob.css'`) is resolved by Vite/Vitest — no special handling needed; if Vitest complains about the CSS import, add `css: false` is NOT correct; instead rely on the default behavior. If a CSS resolution error actually occurs at test time, the executor MUST instead add `css: { include: [] }` to `vitest.config.ts` `test` block — but ONLY if test failure proves it's needed. Do not add it preemptively.
  </action>
  <verify>
    <automated>npm test -- src/components/VolumeKnob.test.tsx --run 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/VolumeKnob.test.tsx` exists.
    - `grep -c "vi.spyOn(document, 'addEventListener')" src/components/VolumeKnob.test.tsx` is at least 1.
    - `grep -c "vi.spyOn(document, 'removeEventListener')" src/components/VolumeKnob.test.tsx` is at least 1.
    - `grep -cE "it\\(['\"]" src/components/VolumeKnob.test.tsx` is at least 4 (four `it(...)` blocks).
    - `grep -c "mousemove" src/components/VolumeKnob.test.tsx` is at least 4 (each of the four tests references mousemove).
    - `grep -c "touchmove" src/components/VolumeKnob.test.tsx` is at least 2 (Tests 1 and 2 at minimum).
    - `npm test -- src/components/VolumeKnob.test.tsx --run` exits 0 with 4 passing tests.
    - `npm test -- --run` exits 0 — full suite green.
  </acceptance_criteria>
  <done>Four tests in `src/components/VolumeKnob.test.tsx` pass and prove MEM-02 (document listeners are attached on drag start, removed on drag end, balanced across repeated cycles, and cleaned up on mid-drag unmount).</done>
</task>

</tasks>

<verification>
- `npm run type-check` exits 0 (new hook, new tests, updated config all type-check)
- `npm run lint` exits 0 (no new lint errors)
- `npm test -- --run` exits 0 with all original tests PLUS:
  - 5 new tests in `src/hooks/useMIDIInput.test.tsx` (covers MEM-01, MEM-03)
  - 4 new tests in `src/components/VolumeKnob.test.tsx` (covers MEM-02)
- The new hook `src/hooks/useMemoryProfiler.ts` is available for Phase 6 (VER-01) — exports `useMemoryProfiler` and `MemoryMetrics`.
- Vitest environment is `jsdom` and `setupFiles` is wired.
</verification>

<success_criteria>
- MEM-01 verification: Tests assert `midiHandler.setNoteOnHandler` is called exactly once on mount and NOT again when `config.throughEnabled` or `config.latencyCompensation` changes — both tests pass.
- MEM-02 verification: Tests assert document listeners are added/removed in balanced counts across drag cycles and cleaned up on mid-drag unmount — all tests pass.
- MEM-03 verification: The "note handler reads fresh config.throughEnabled via ref" test passes, proving the handler reads `configRef.current` at invocation time, not the value captured at mount.
- `useMemoryProfiler` is exported from `src/hooks/useMemoryProfiler.ts` and can be imported into UI components or Phase 6 VER-01 tests.
- All 93 pre-existing tests still pass under the new jsdom environment.
</success_criteria>

<output>
After completion, create `.planning/phases/01-react-cleanup/01-02-SUMMARY.md` documenting:
- Dependencies installed (versions and total install size note from `npm install` output)
- Vitest config changes (node → jsdom, new setupFiles)
- The `useMemoryProfiler` hook's public API (interface signature, usage example)
- Test count: 5 new in useMIDIInput.test.tsx + 4 new in VolumeKnob.test.tsx = 9 new tests
- Confirmation that `npm run type-check`, `npm run lint`, and `npm test -- --run` all exit 0
- Total test count before / after (was 93, should be 102)
- Hand-off note for Phase 6 (VER-01): `useMemoryProfiler` is the verification primitive for the 1-hour leak test
- Any deviations from the plan (e.g., if CSS resolution forced a `vitest.config.ts` adjustment — document explicitly)
</output>

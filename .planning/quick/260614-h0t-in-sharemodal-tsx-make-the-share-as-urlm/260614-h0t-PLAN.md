---
phase: quick-260614-h0t
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/production/ShareModal.tsx
  - src/components/production/ShareModal.test.tsx
autonomous: true
requirements: [SHARE-URLMODE-DEFAULT]

must_haves:
  truths:
    - "On open (Link tab active), the Share as toggle defaults to Editor"
    - "Switching to the Embed tab resets the toggle to Embed"
    - "Switching to Link, Social, QR, or Email resets the toggle to Editor"
    - "The user can still manually toggle Embed/Editor within the current tab"
    - "The toggle remains visible on every tab"
  artifacts:
    - path: "src/components/production/ShareModal.tsx"
      provides: "Per-tab default urlMode via helper + tab-change effect"
      contains: "defaultUrlModeForTab"
    - path: "src/components/production/ShareModal.test.tsx"
      provides: "Tests covering per-tab default behavior"
      contains: "defaultUrlModeForTab"
  key_links:
    - from: "src/components/production/ShareModal.tsx (activeTab effect)"
      to: "setUrlMode"
      via: "useEffect keyed on [activeTab]"
      pattern: "setUrlMode\\(defaultUrlModeForTab"
---

<objective>
Make the "Share as:" urlMode toggle default based on the active tab in ShareModal: 'editor' for Link/Social/QR/Email tabs, 'embed' only for the Embed tab. On each tab switch, urlMode resets to the tab-appropriate default; the manual toggle still works within a tab, and the toggle stays visible on all tabs.

Purpose: The Embed tab is the only context where an embed-view URL is the sensible default. Sharing a link/social/QR/email should default to the editable editor view.
Output: Updated ShareModal.tsx with per-tab default logic, plus extended ShareModal.test.tsx.
</objective>

<execution_context>
@/Users/adar/Code/groovy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adar/Code/groovy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@src/components/production/ShareModal.tsx
@src/components/production/ShareModal.test.tsx

<interfaces>
<!-- Already in ShareModal.tsx — use these directly, no exploration needed. -->

type ShareTab = 'link' | 'social' | 'embed' | 'qr' | 'email';

// Current state (line ~46-48):
const [activeTab, setActiveTab] = useState<ShareTab>('link');
const [urlMode, setUrlMode] = useState<'embed' | 'editor'>('embed');

// shareableURL is derived from urlMode via useMemo (line ~60-63).
// An existing effect resets short URLs when shareableURL changes (line ~68-72).
// An existing QR auto-shorten effect is keyed on [activeTab, shareableURL, urlMode] (line ~75-103).

// From src/core/GrooveURLCodec.ts:
//   getShareableURL(groove, baseURL?, mode='embed'): string
//   mode === 'embed' => URL + '&embed=true'; mode === 'editor' => URL without '&embed=true'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Derive urlMode default from active tab</name>
  <files>src/components/production/ShareModal.tsx</files>
  <action>
Add a module-level pure helper near the top of the file (after the ShareTab type or after TABS), with an explicit return type:

  function defaultUrlModeForTab(tab: ShareTab): 'embed' | 'editor' {
    return tab === 'embed' ? 'embed' : 'editor';
  }

(2-space indent, named function, explicit return type per CLAUDE.md conventions.)

Initialize urlMode from the helper so initial render matches the initial tab. Change line ~48 from `useState<'embed' | 'editor'>('embed')` to `useState<'embed' | 'editor'>(defaultUrlModeForTab('link'))`. Since activeTab initializes to 'link', this yields 'editor' as the initial value (a deliberate behavior change from the current 'embed' initial).

Add a useEffect keyed on [activeTab] that applies the tab default on every tab change:

  useEffect(() => {
    setUrlMode(defaultUrlModeForTab(activeTab));
  }, [activeTab]);

Place this effect alongside the other effects (after the existing short-URL reset effect at line ~68-72 and before or after the QR auto-shorten effect at line ~75-103 — order does not matter for correctness). Do NOT add urlMode to this effect's dependency array; it must only fire on activeTab changes, otherwise the manual toggle would be immediately reverted.

Interaction check with the existing QR auto-shorten effect (keyed on [activeTab, shareableURL, urlMode]): when the user switches tabs, this new effect calls setUrlMode, which changes the memoized shareableURL, which retriggers the QR effect and the short-URL reset effect. This is the existing, correct cascade — no loop, because the new effect's dependency is only [activeTab] and activeTab does not change as a result of setUrlMode. Leave the QR auto-shorten effect unchanged.

Leave the toggle JSX (lines ~418-452) and the toggle button onClick handlers (setUrlMode + trackShareModeToggle) unchanged — they keep working as the manual override within a tab, and the toggle stays rendered above the tab navigation for all tabs.
  </action>
  <verify>
    <automated>npx tsc -b 2>&1 | tail -5; grep -c "defaultUrlModeForTab" src/components/production/ShareModal.tsx</automated>
  </verify>
  <done>defaultUrlModeForTab helper exists; urlMode initialized via the helper; a useEffect keyed on [activeTab] calls setUrlMode(defaultUrlModeForTab(activeTab)); toggle JSX and onClick handlers unchanged; tsc -b passes with no errors.</done>
</task>

<task type="auto">
  <name>Task 2: Update tests for per-tab default and fix the now-stale Link-tab assertion</name>
  <files>src/components/production/ShareModal.test.tsx</files>
  <action>
The existing test "copies the displayed share URL instead of a hard-coded embed suffix" (line ~66-88) asserts the displayed Link-tab URL contains `embed=true`. With Task 1, the Link tab now defaults to 'editor', so the displayed URL will NOT contain `embed=true`. This test must be updated.

Update that test: instead of matching `embed=true`, first click the "Embed" mode toggle button (the toggle Button labeled "Embed", NOT the Embed tab span) so the Link tab shows the embed-mode URL, then assert the displayed URL contains `embed=true` and that clicking Copy writes that exact value. To disambiguate the toggle Button from the tab span, select via `screen.getAllByText('Embed')` and find the element whose `tagName === 'BUTTON'` (the tab uses a SPAN for its label, as the existing embed-escaping tests demonstrate). Alternatively, since the test's core intent is "copy the displayed value, not a hardcoded suffix", you may instead drop the `embed=true` substring requirement and assert against the URL actually shown in the input (use `screen.getByDisplayValue((v) => v.includes('?'))` or read the input's current value) — but the cleaner approach is to toggle to Embed mode first so intent stays explicit. Pick the toggle-to-Embed approach.

Add a new describe block "per-tab urlMode default" with these tests. Use the existing tab/toggle selection patterns already in the file (find the hidden SPAN for tab labels; find the BUTTON for toggle labels). The "Share as:" description text is a reliable signal of current mode: 'embed' shows "Embed view (optimized for viewing)" and 'editor' shows "Editor view (allows editing)" (see ShareModal.tsx lines ~422-426).

  1. "defaults to Editor on the initial Link tab": render with default props; assert the description text "Editor view (allows editing)" is present (mode = editor).
  2. "defaults to Embed when the Embed tab is active": render, click the Embed tab span, assert "Embed view (optimized for viewing)" is present.
  3. "resets to Editor when switching from Embed tab back to Link tab": render, click Embed tab span (mode becomes embed), then click the Link tab span, assert "Editor view (allows editing)" is present.
  4. "manual toggle still overrides within a tab": render (Link tab, editor default), click the "Embed" toggle BUTTON, assert "Embed view (optimized for viewing)" is present (manual override held within the tab; no tab switch occurred).

For the QR tab, the existing test "calls shortenURL ... QR tab activates" asserts the shortened URL `toContain('embed=true')`. With QR now defaulting to 'editor', the QR-mode URL will NOT contain `embed=true`. Update that assertion: change `expect(vi.mocked(shortenURL).mock.calls[0][0]).toContain('embed=true')` to assert the call argument is a non-empty share URL that does NOT contain `embed=true` (e.g. `expect(arg).not.toContain('embed=true')` and `expect(arg.length).toBeGreaterThan(0)`), reflecting the editor-mode default on the QR tab.

Do not change the embed-escaping tests (they click the Embed tab, which yields embed mode — still correct). Verify those still pass.
  </action>
  <verify>
    <automated>npx vitest run src/components/production/ShareModal.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>All tests in ShareModal.test.tsx pass. New "per-tab urlMode default" describe block covers initial Editor default, Embed-tab Embed default, reset-on-tab-switch, and manual-override-within-tab. The previously embed-assuming Link-tab copy test and the QR shorten test are updated for the new editor default.</done>
</task>

</tasks>

<verification>
- `npx tsc -b` passes with no errors.
- `npx vitest run src/components/production/ShareModal.test.tsx` passes (all existing + new tests).
- Manual smoke (optional): open Share modal → toggle shows Editor on Link; click Embed tab → toggle shows Embed; click Link → back to Editor; manually click Embed toggle on Link → stays Embed until tab change.
</verification>

<success_criteria>
- urlMode defaults to 'editor' for Link/Social/QR/Email and 'embed' for the Embed tab.
- Each tab switch resets urlMode to that tab's default.
- Manual Embed/Editor toggle still works within the active tab.
- Toggle remains visible on all tabs.
- Tests cover per-tab defaults; suite is green; build clean.
</success_criteria>

<output>
After completion, create `.planning/quick/260614-h0t-in-sharemodal-tsx-make-the-share-as-urlm/260614-h0t-SUMMARY.md`
</output>

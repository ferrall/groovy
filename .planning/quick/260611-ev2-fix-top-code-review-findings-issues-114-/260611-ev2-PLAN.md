---
phase: quick-260611-ev2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/production/ShareModal.tsx
  - src/components/production/ShareModal.test.tsx
  - src/hooks/useMIDIInput.ts
  - src/hooks/useMIDIInput.test.tsx
  - src/midi/PerformanceTracker.ts
  - src/midi/PerformanceTracker.test.ts
  - src/core/GrooveEngine.ts
  - src/core/GrooveEngine.test.ts
  - src/hooks/useMIDITracking.ts
  - src/hooks/useMIDITracking.test.ts
  - src/core/GrooveStorage.ts
  - src/core/GrooveStorage.test.ts
autonomous: true
requirements: [ISSUE-114, ISSUE-115, ISSUE-116, ISSUE-117, ISSUE-118]

must_haves:
  truths:
    - "Embed snippet titles containing quotes or angle brackets cannot break out of the title attribute"
    - "Re-rendering useMIDIInput with a late-provided synth adopts it without constructing a second DrumSynth"
    - "A hit slightly before a downbeat reports a small negative timing error, not a huge late error"
    - "PerformanceTracker uses the audio-start clock anchor from GrooveEngine when available"
    - "When localStorage quota is exceeded, the oldest grooves are dropped and the save succeeds on retry"
  artifacts:
    - path: "src/components/production/ShareModal.tsx"
      provides: "HTML-escaped embed title"
      contains: "escapeXml"
    - path: "src/core/GrooveEngine.ts"
      provides: "getPlayStartPerformanceTime getter"
      contains: "getPlayStartPerformanceTime"
    - path: "src/core/GrooveStorage.test.ts"
      provides: "quota-exceeded cleanup test"
      contains: "quotaExceeded"
  key_links:
    - from: "src/hooks/useMIDITracking.ts"
      to: "GrooveEngine.getPlayStartPerformanceTime"
      via: "anchor passed to performanceTracker.enable"
      pattern: "getPlayStartPerformanceTime"
    - from: "src/components/production/ShareModal.tsx"
      to: "escapeXml"
      via: "import from core ExportUtils"
      pattern: "escapeXml\\(grooveTitle\\)"
---

<objective>
Fix the top five code-review findings (GitHub issues #114–#118). Each fix is a focused, independent change with new or extended tests. One atomic commit per issue.

Purpose: Close known correctness, security, and reliability defects in MIDI timing, sharing, and storage paths. Drummers must trust real-time feedback; these fixes remove a stale-clock anchor, a beat-boundary quantization error, a duplicate AudioContext leak, an XSS vector, and a no-op storage cleanup.

Output: 5 source edits, 5 test additions/extensions, 5 commits. Full suite (`npx vitest run`) stays green throughout — baseline is 155/155.
</objective>

<execution_context>
@/Users/adar/Code/groovy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adar/Code/groovy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase — no exploration required. -->

From src/core/ExportUtils.ts (already exported):
```typescript
export function escapeXml(str: string): string; // escapes & < > " '
```

From src/components/production/ShareModal.tsx:
- line 61:  const grooveTitle = groove.title || 'Drum Groove';
- line 95-98: embedCode useMemo builds the iframe string with `title="${grooveTitle}"`.
- ShareModal imports from '../../core' already (line 25). escapeXml is re-exported from src/core (verify via barrel; if not, import from '../../core/ExportUtils').

From src/hooks/useMIDIInput.ts:
- line 42: export function useMIDIInput(synth?: DrumSynth): UseMIDIInputReturn
- line 44: const synthRef = useRef<DrumSynth>(synth || new DrumSynth());  // BUG: runs `new DrumSynth()` every render while synth is undefined.

From src/midi/PerformanceTracker.ts:
- line 186-210: private calculateTimingError(timestamp) — finds nearest offset within current beat only; never considers next beat downbeat (offset === beatDurMs).
- `this.beatOffsets` is the in-beat grid; `beatDurMs = (60 / this.tempo) * 1000`.

From src/core/GrooveEngine.ts:
- line 44: private startTime = 0;
- line 285: async play(groove, loop): sets `this.startTime = this.synth.getCurrentTime()` at line 294.
- line 614: stop(): sets `this.isPlaying = false` (clear the anchor here too).
- line 72: getSynth(): DrumSynth — pattern for getters.

From src/hooks/useMIDITracking.ts:
- line 34: playStartTimeRef.current = performance.now(); — captured at effect time, NOT audio start.
- line 36: performanceTracker.enable(groove, playStartTimeRef.current);
- Hook signature must be extended to receive the engine (or the anchor). useGrooveEngine already returns `engine`.

From src/core/GrooveStorage.ts:
- line 111-123: setItem → on quotaExceeded calls `safeStorage.cleanup('groove-')` (NO-OP: all grooves live in one key `groovy-my-grooves` / STORAGE_KEY).
- SavedGroove has `modifiedAt: number` (line 38); list is sorted desc by modifiedAt (line 168).
- safeStorage imported as namespace object (line 12) — mockable in tests via `vi.mock('../utils/safeStorage')`.
- STORAGE_KEY constant holds the single key.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Escape groove title in embed snippet (#114)</name>
  <files>src/components/production/ShareModal.tsx, src/components/production/ShareModal.test.tsx</files>
  <behavior>
    - Test: embedCode for a groove titled `"><script>alert(1)</script>` contains no raw `<script>` and no attribute-breaking `"` inside the title attribute (entities `&quot; &lt; &gt;` present instead).
    - Test: a plain title (no special chars) renders unchanged inside the title attribute.
  </behavior>
  <action>Wrap the title in the embed iframe string with `escapeXml(...)` (the existing helper exported from src/core/ExportUtils.ts). At line ~97 change `title="${grooveTitle}"` to `title="${escapeXml(grooveTitle)}"`. Import escapeXml from '../../core' if the barrel re-exports it (check src/core/index.ts); otherwise import from '../../core/ExportUtils'. Add tests to the existing ShareModal.test.tsx asserting the escaped output. Per ISSUE-114. Do not change any other use of grooveTitle (social/email already use encodeURIComponent or are plain text contexts).</action>
  <verify>
    <automated>npx vitest run src/components/production/ShareModal.test.tsx</automated>
  </verify>
  <done>Embed snippet escapes &lt; &gt; &quot; in the title; new tests pass; full `npx vitest run` green. Commit: `fix: escape groove title in embed snippet (#114)`</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Eliminate duplicate DrumSynth in useMIDIInput (#115)</name>
  <files>src/hooks/useMIDIInput.ts, src/hooks/useMIDIInput.test.tsx</files>
  <behavior>
    - Test: rendering useMIDIInput with no synth then re-rendering does NOT construct more than one fallback DrumSynth.
    - Test: rendering with synth undefined, then re-rendering with a provided synth, adopts the provided synth (synthRef.current === provided) and constructs no extra DrumSynth.
  </behavior>
  <action>Replace the eager `useRef<DrumSynth>(synth || new DrumSynth())` (line 44) with a lazy initializer so the fallback is constructed at most once and only when no synth is provided. Use the lazy-ref pattern: initialize `synthRef` to the provided `synth` if present, else `null`, then in a `useEffect`/initialization guard construct the fallback only once if still null. Add an effect that adopts a late-provided synth: `if (synth && synthRef.current !== synth) synthRef.current = synth;`. Ensure downstream uses of `synthRef.current` remain non-null when needed (guard the fallback construction to run before first use). Keep explicit types, no `any`. Per ISSUE-115. Mock DrumSynth in the test (spy on its constructor) to count instantiations; extend existing useMIDIInput.test.tsx.</action>
  <verify>
    <automated>npx vitest run src/hooks/useMIDIInput.test.tsx</automated>
  </verify>
  <done>Only one DrumSynth ever constructed; provided synth adopted on later render; new tests pass; full `npx vitest run` green. Commit: `fix: avoid duplicate DrumSynth in useMIDIInput (#115)`</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Beat-boundary quantization in PerformanceTracker (#116)</name>
  <files>src/midi/PerformanceTracker.ts, src/midi/PerformanceTracker.test.ts</files>
  <behavior>
    - Test: with beatDurMs=500, a hit at posInBeat=490 reports errorMs ≈ -10 (small negative, slow but near next downbeat) with high accuracy — NOT a large positive late error.
    - Test: a hit just after a downbeat (posInBeat≈10) reports errorMs ≈ +10.
    - Test: swing-grid boundary case — nearest-offset selection still correct when the wrapped next-downbeat candidate is closest.
  </behavior>
  <action>In `calculateTimingError` (lines 186-210), add `beatDurMs` as an additional wrap candidate when finding the nearest offset. After the loop over `this.beatOffsets`, also compare distance to `beatDurMs` (the next beat's downbeat). If `beatDurMs` is the nearest, the signed error must be computed against it: `errorMs = posInBeat - beatDurMs` (e.g. posInBeat=490, beatDurMs=500 → -10). Keep the signed convention (negative=slow/early-before-downbeat, positive=fast/late-after-downbeat) consistent with existing usage and the spec comment. Preserve `absError = Math.abs(errorMs)`. Explicit return type unchanged. Per ISSUE-116. Add the three test cases to the existing PerformanceTracker.test.ts; instantiate/enable the tracker with a known tempo so beatDurMs is deterministic.</action>
  <verify>
    <automated>npx vitest run src/midi/PerformanceTracker.test.ts</automated>
  </verify>
  <done>Early-before-downbeat hits report small signed errors; all three new cases pass; full `npx vitest run` green. Commit: `fix: quantize MIDI hits to next beat boundary (#116)`</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Audio-start clock anchor for tracking (#117)</name>
  <files>src/core/GrooveEngine.ts, src/core/GrooveEngine.test.ts, src/hooks/useMIDITracking.ts, src/hooks/useMIDITracking.test.ts</files>
  <behavior>
    - Test (GrooveEngine): after play(), getPlayStartPerformanceTime() returns a number captured at the same instant as startTime; after stop() it returns null (cleared). Document stable semantics: value persists across the playback session, cleared on stop.
    - Test (useMIDITracking): when an engine exposing getPlayStartPerformanceTime() is provided, performanceTracker.enable is called with that anchor (not performance.now()). When anchor is null/engine absent, falls back to performance.now().
  </behavior>
  <action>GrooveEngine (React-free — no React imports): add a private field `playStartPerformanceTime: number | null = null`. In `play()` at the line that sets `this.startTime = this.synth.getCurrentTime()` (line 294), capture `this.playStartPerformanceTime = performance.now()` at the same instant (immediately adjacent). In `stop()` (line 614) set `this.playStartPerformanceTime = null`. Add getter `getPlayStartPerformanceTime(): number | null { return this.playStartPerformanceTime; }`. Create src/core/GrooveEngine.test.ts (does not exist) — mock/stub DrumSynth's getCurrentTime/resume so play() runs in test env; assert getter behavior on play/stop.

useMIDITracking: extend the hook to accept the engine (or its anchor) — the caller (useGrooveEngine) already returns `engine`. Read `engine?.getPlayStartPerformanceTime?.() ?? null`; if non-null use it as the enable() anchor, else keep `performance.now()` fallback (line 34/36). Set `playStartTimeRef.current` to whichever was used. Create/extend useMIDITracking.test.ts asserting enable() receives the engine anchor when provided and the fallback otherwise. Per ISSUE-117. Keep core React-free; the anchor wiring lives only in the hook.</action>
  <verify>
    <automated>npx vitest run src/core/GrooveEngine.test.ts src/hooks/useMIDITracking.test.ts</automated>
  </verify>
  <done>Engine exposes/clears the anchor; tracking uses it with documented fallback; new tests pass; core has no React imports; full `npx vitest run` green. Commit: `fix: anchor MIDI tracking to audio start time (#117)`</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Real quota cleanup in GrooveStorage (#118)</name>
  <files>src/core/GrooveStorage.ts, src/core/GrooveStorage.test.ts</files>
  <behavior>
    - Test (mock safeStorage so first setItem returns {success:false, quotaExceeded:true}, second returns {success:true}): saving with many existing grooves drops the oldest ~25% (by modifiedAt), keeps the newest grooves AND the groove being saved, retries once, returns {success:true}.
    - Test: if retry still fails, returns {success:false} with the friendly "Storage full" error.
    - Test: the groove currently being saved is never dropped even if it is oldest.
  </behavior>
  <action>Replace the no-op `safeStorage.cleanup('groove-')` (line 116) with array-level trimming. On quotaExceeded: sort the in-memory `grooves` array by modifiedAt ascending (oldest first), compute drop count = floor(grooves.length * 0.25) (at least 1 if length > 1), and remove that many oldest entries — but never remove the groove just added/updated (savedGroove / existingId). Re-serialize and retry setItem(STORAGE_KEY, ...) once. If retry succeeds return {success:true, groove: savedGroove}; if it still fails return the existing friendly error object. Use logger.warn for the cleanup notice (already present). Explicit return types, null over undefined, no `any`. Per ISSUE-118. Create src/core/GrooveStorage.test.ts (does not exist) — `vi.mock('../utils/safeStorage')` to drive the quota-exceeded path and capture the JSON written on retry; assert which grooves survived.</action>
  <verify>
    <automated>npx vitest run src/core/GrooveStorage.test.ts</automated>
  </verify>
  <done>Quota cleanup actually trims oldest grooves and save succeeds on retry; saved groove always survives; friendly error on persistent failure; new tests pass; full `npx vitest run` green. Commit: `fix: trim oldest grooves on storage quota exceeded (#118)`</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL/groove title → embed HTML | User- or URL-controllable groove title is interpolated into an HTML iframe snippet copied/pasted by users |
| localStorage → app | Persisted groove data read back and parsed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-ev2-01 | Tampering/Elevation (XSS) | ShareModal embed snippet (title attr) | mitigate | Task 1: escape title with escapeXml so quotes/angle brackets cannot break out of the attribute |
| T-ev2-02 | Denial of Service | GrooveStorage save under quota pressure | mitigate | Task 5: real array-level trim frees space so saves succeed instead of silently failing |
| T-ev2-03 | Information disclosure | localStorage groove contents | accept | Local-only, no PII, single-user browser storage |
</threat_model>

<verification>
- Establish baseline first: `npx vitest run` reports 155/155 pass before any edit.
- After EACH task: `npx vitest run` (full suite) must pass with no regressions; the new tests for that issue are included.
- After all tasks: 5 commits exist, each `fix: ... (#NNN)`, suite green.
- `grep -rn "from 'react'" src/core/GrooveEngine.ts` returns nothing (core stays React-free).
</verification>

<success_criteria>
- All five issues (#114–#118) fixed per their scoped descriptions.
- Each fix has new/extended tests proving the behavior.
- Full suite never regresses; final state green (≥155 tests, plus new ones).
- Five atomic commits, one per issue, with issue numbers in messages.
- CLAUDE.md conventions honored: logger (not console), explicit return types, null over undefined, no `any`, core modules React-free.
</success_criteria>

<output>
After completion, create `.planning/quick/260611-ev2-fix-top-code-review-findings-issues-114-/260611-ev2-SUMMARY.md`
</output>

---
phase: quick-260611-g5v
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useMIDITimingAccuracy.ts
  - src/midi/PerformanceTracker.ts
  - src/midi/MIDIAccess.ts
  - src/hooks/useMIDITracking.ts
  - src/hooks/useMIDITracking.test.ts
  - src/hooks/useGrooveEngine.ts
  - vite.config.ts
  - src/core/GrooveEngine.ts
  - src/core/GrooveEngine.test.ts
  - src/components/production/ShareModal.tsx
  - src/midi/MIDIHandler.ts
  - src/midi/KeyboardMIDISimulator.ts
  - src/hooks/useMIDIInput.ts
  - src/utils/safeStorage.ts
  - src/utils/latencyStorage.ts
  - src/core/GrooveURLCodec.ts
  - src/core/GrooveURLCodec.test.ts
  - CLAUDE.md
autonomous: true
requirements: [PERF-01, PERF-02]
must_haves:
  truths:
    - "Full test suite (199 tests) stays green after every commit"
    - "MIDI hot paths no longer call console.* unconditionally"
    - "The midi-note-hit listener subscribes once per playback session, not per position change"
    - "Non-loop playback fires playbackStateChange(false) no earlier than the groove's full duration"
    - "abcjs ships in its own bundle chunk; main chunk shrinks below the 810kB warning"
    - "Latency config migrates from the snake_case key to kebab-case without data loss"
    - "GrooveURLCodec round-trips empty-middle-measure patterns without shifting alignment, backward compatible with existing URLs"
  artifacts:
    - path: "src/utils/logger.ts"
      provides: "Debug-gated logging used by hot paths (existing)"
    - path: "vite.config.ts"
      provides: "manualChunks entry isolating abcjs"
    - path: "src/hooks/useMIDITracking.test.ts"
      provides: "Regression test: listener not re-added on position change"
    - path: "src/core/GrooveEngine.test.ts"
      provides: "Regression test: deferred stop in non-loop mode"
    - path: "src/core/GrooveURLCodec.test.ts"
      provides: "Regression test: empty-middle-measure + existing-URL compat"
  key_links:
    - from: "src/midi/PerformanceTracker.ts"
      to: "src/utils/logger.ts"
      via: "import { logger }"
      pattern: "logger\\.(log|warn|error)"
    - from: "src/utils/latencyStorage.ts"
      to: "localStorage"
      via: "migration read-old/write-new/remove-old on load"
      pattern: "groovy-midi-latency-config"
---

<objective>
Implement the 16-item performance & cleanup checklist from GitHub issue #125, grouped into 6 cohesive atomic commits. Each commit must leave the full test suite green (`npx vitest run`) and be self-contained.

Purpose: The MIDI feedback path must be rock-solid — no hot-path logging overhead, no listener accumulation, no early playback stop, no stale config keys. Cleanup removes dead code and convention violations that erode trust in the timing engine.

Output: 6 commits (perf/refactor/fix/docs, each `(#125)`-suffixed); new/extended tests for every behavioral change (P2, P4, P6, C4, C7, C8, C9); a smaller main bundle chunk.
</objective>

<execution_context>
@/Users/adar/Code/groovy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/adar/Code/groovy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

<key_findings>
Pre-verified facts (line numbers shifted since issue authored — re-verify on read):

- P3 / abcjs: The heavy `abcjs` module is imported ONLY in `src/core/ABCRenderer.ts` (`import abcjs from 'abcjs'`; value usage `abcjs.renderAbc`, type usages `abcjs.AbcVisualParams` / `abcjs.TuneObject`). Every other reference (SheetMusicDisplay, SVGAnnotationLayout, MIDITrackingDebug, useMIDITrackingFeedback, ABCConstants, CSS) uses only `.abcjs-*` CSS class-string selectors — NO module import.
- `renderABC` is SYNCHRONOUS and called in 4 places: `src/core/ExportUtils.ts`, `src/components/SheetMusicDisplay.tsx`, `src/components/production/PrintPreviewModal.tsx`, plus re-export in `src/core/index.ts`. Converting to dynamic `import('abcjs')` would make renderABC async and force async plumbing across all 4 call sites + tests (>5 files) — the balloon scenario the issue warns about. THEREFORE: implement P3 via vite `manualChunks` (`'abcjs': ['abcjs']`) and note the dynamic-import deferral in the summary. An established manualChunks map already exists in vite.config.ts.
- C4 / timingErrors: `stats.timingErrors` (PerformanceTracker.ts) is consumed ONLY by `src/midi/PerformanceTracker.test.ts` (3 refs). `getStats` consumers grep also hits `safeStorage.ts`/`GrooveStorage.test.ts` but those are `getStorageStats` (unrelated). MIDITrackingDebug does NOT read timingErrors. Least-blast-radius choice: rename field `timingErrors` → `timingScores` (it stores accuracy scores, not ms). Update PerformanceStats interface + the 4 internal usages + 3 test refs.
- C5 / GroovePattern: `[key: string]: any` index signature at PerformanceTracker.ts ~line 40. Remove (no-any convention).
- C8 / codec: `MEASURE_SEP = '|'`. Two call sites use `.split(MEASURE_SEP).filter(p => p.length > 0)` at ~line 297 and ~line 429. Filtering empties shifts alignment when a middle measure is empty.
- C6: `process.env.NODE_ENV === 'development'` at MIDIHandler.ts lines ~61, ~81, ~96.
- C9: `' '` → Hi-hat (note 46) in DEFAULT_KEYBOARD_MIDI_CONFIG (KeyboardMIDISimulator.ts ~line 23); log hints in MIDIAccess.ts ~line 150 and useMIDIInput.ts ~line 164 say "Space=Hi-hat".
- C10: CLAUDE.md line 64 stale-claims "dompurify ... for ShareModal and display". README/docs have no dompurify mention.
- safeStorage QuotaExceededError handling is depended on by GrooveStorage tests — keep identical.
- vitest sets `import.meta.env.DEV = true` by default, so C6 works in tests.
</key_findings>

<interfaces>
PerformanceStats (src/midi/PerformanceTracker.ts ~line 16):
```typescript
export interface PerformanceStats {
  // ...
  timingErrors: number[];   // RENAME to timingScores
  // ...
}
```

GroovePattern (src/midi/PerformanceTracker.ts ~line 36):
```typescript
export interface GroovePattern {
  // ...fields...
  [key: string]: any;       // REMOVE
}
```

logger (src/utils/logger.ts):
```typescript
export const logger: {
  log(...args: unknown[]): void;   // debug-gated
  warn(...args: unknown[]): void;  // debug-gated
  error(...args: unknown[]): void; // always
  isDebugMode(): boolean;
};
```

useGrooveEngine return surface already exports: play, stop, togglePlayback, updateGroove, setSyncMode, getSyncMode, playPreview (currently plain functions) plus metronome setters (already useCallback) — keep the exported API identical.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (Commit 1 — perf: P1 hot-path logging): route MIDI hot paths through logger</name>
  <files>src/hooks/useMIDITimingAccuracy.ts, src/midi/PerformanceTracker.ts, src/midi/MIDIAccess.ts</files>
  <action>
Replace unconditional console.* calls in the MIDI hot path with the debug-gated `logger` (src/utils/logger).
- useMIDITimingAccuracy.ts: import `{ logger }` from '../utils/logger'; replace the two `console.log(...)` calls (the "Playback stopped. Average timing" log and the per-hit "Timing Accuracy" scaling log) with `logger.log(...)`.
- PerformanceTracker.ts: import `{ logger }` (src/midi already imports from src/types and src/utils is React-free, so this is allowed). Replace each `console.warn(...)` in enable/disable/validation with `logger.warn(...)` and each `console.log(...)` (enable/disable status) with `logger.log(...)`.
- MIDIAccess.ts: import `{ logger }`; map all `console.log`/`console.info` → `logger.log`, `console.warn` → `logger.warn`, `console.error` → `logger.error`. Preserve the exact message strings (the Space=Hi-hat hint string is rebound later in Commit 5 — do not change wording here).
Do not introduce any `any`. Explicit return types unchanged. Keep src/midi React-free.
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <done>No unconditional `console.` calls remain in useMIDITimingAccuracy.ts, PerformanceTracker.ts, or MIDIAccess.ts (verify: `grep -rn "console\." src/hooks/useMIDITimingAccuracy.ts src/midi/PerformanceTracker.ts src/midi/MIDIAccess.ts` returns nothing). Suite green. Commit: `perf: route MIDI hot-path logging through debug-gated logger (#125)`</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (Commit 2 — perf: P2 listener churn + P5 hook memoization)</name>
  <files>src/hooks/useMIDITracking.ts, src/hooks/useMIDITracking.test.ts, src/hooks/useGrooveEngine.ts</files>
  <behavior>
    - P2 test (extend useMIDITracking.test.ts): with trackingEnabled && isPlaying held constant, changing `currentPosition` (and `groove.tempo`) across rerenders does NOT call window.addEventListener('midi-note-hit', ...) more than once (spy on window.addEventListener, filter by event name).
    - P2 test: a dispatched 'midi-note-hit' still produces a 'midi-tracking-hit' event whose detail carries the CURRENT position and tempo (latest values via refs), not stale ones.
    - P5: useGrooveEngine still exports the same API; identities of play/stop/togglePlayback/updateGroove/setSyncMode/getSyncMode/playPreview are stable across rerenders (referential equality between renders when deps unchanged).
  </behavior>
  <action>
P2 (useMIDITracking.ts): Eliminate per-position re-subscription. Keep latest `currentPosition`, `groove.tempo`, and `groove` in refs updated by a cheap effect that runs on every render (e.g. `positionRef.current = currentPosition` each render or in a deps-tracked effect). Change the 'midi-note-hit' listener effect deps to `[trackingEnabled, isPlaying]` ONLY, so it subscribes once per (trackingEnabled && isPlaying) session. Inside the handler, read position/tempo from refs. Behavior of dispatched 'midi-tracking-hit' events (detail shape and values) MUST be unchanged — it must still carry the current position/tempo. Keep using `logger` (already imported).
P5 (useGrooveEngine.ts): Wrap `play`, `stop`, `togglePlayback`, `updateGroove`, `setSyncMode`, `getSyncMode`, `playPreview` in useCallback for stable identities. `togglePlayback` and `updateGroove` read `isPlaying`: prefer functional/ref patterns or correct deps so identity stays as stable as possible without breaking behavior. `togglePlayback` depends on `play`/`stop`; ensure those are memoized first so the dependency chain is stable. Do NOT change the exported API. The centralized sync effect in ProductionPage depends on `updateGroove` identity — stable identity makes it run less (the goal); do not break it.
No `any`; explicit return types; src/core+src/midi stay React-free (these are hooks, fine).
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <done>New P2 tests pass: listener subscribed once despite position changes; events still carry current position/tempo. useGrooveEngine action identities stable. Full suite green. Commit: `perf: subscribe MIDI listener once per session and memoize engine actions (#125)`</done>
</task>

<task type="auto">
  <name>Task 3 (Commit 3 — perf: P3 abcjs bundle split via manualChunks)</name>
  <files>vite.config.ts</files>
  <action>
Add `'abcjs': ['abcjs']` to the existing `build.rollupOptions.output.manualChunks` map (alongside jspdf/midi-writer/qrcode/lamejs). This isolates abcjs into its own chunk so the main chunk shrinks below the 810kB `chunkSizeWarningLimit`.
Rationale (record in SUMMARY): the dynamic-`import('abcjs')` refactor was DEFERRED because `renderABC` is synchronous and consumed at 4 call sites (ExportUtils, SheetMusicDisplay, PrintPreviewModal, index barrel) — converting it to async would require async plumbing across >5 files, exceeding the issue's balloon threshold. manualChunks achieves the bundle goal (own chunk + smaller main) with zero source churn.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tee /tmp/g5v-build.log; grep -i "abcjs" /tmp/g5v-build.log; echo "--- warnings ---"; grep -i "chunks are larger than\|810" /tmp/g5v-build.log || echo "no oversize-chunk warning"</automated>
  </verify>
  <done>Build succeeds. An `abcjs` chunk appears in build output. The main/index chunk is smaller than before and the >810kB chunk warning is gone (or only abcjs's own chunk, if any, is flagged separately). Suite still green (`npx vitest run`). Commit: `perf: split abcjs into its own bundle chunk (#125)`</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4 (Commit 4 — fix: P6 deferred non-loop stop + C1 dead scheduledNotes)</name>
  <files>src/core/GrooveEngine.ts, src/core/GrooveEngine.test.ts</files>
  <behavior>
    - P6 test (extend GrooveEngine.test.ts, fake timers + mocked synth getCurrentTime, follow existing pattern): in non-loop mode, `playbackStateChange(false)` is NOT emitted until at least the groove's full duration (`totalPositions * noteDuration`) has elapsed — i.e. not up to ~150ms early when the last note is merely scheduled.
    - C1: deleting scheduledNotes field + its clear loop does not break any existing GrooveEngine test.
  </behavior>
  <action>
P6 (GrooveEngine.ts): In the scheduling loop, when `absolutePosition === totalPositions - 1 && !this.loopEnabled`, do NOT call `this.stop()` immediately. Instead compute the actual groove end time and schedule the stop for then: store a timer id (e.g. `this.endStopTimerID = window.setTimeout(() => this.stop(), delayMs)`) where `delayMs` derives from the real end time relative to current audio time (`this.startTime + totalPositions * noteDuration` converted vs `this.synth.getCurrentTime()`), so the final note's audio actually plays before stop. Clear `this.endStopTimerID` inside `stop()` (and on a fresh `play()`), mirroring how `this.timerID` is cleared, to avoid stale/duplicate stops.
C1 (GrooveEngine.ts): Remove the dead `private scheduledNotes: number[] = [];` field and the `this.scheduledNotes.forEach((id) => clearTimeout(id)); this.scheduledNotes = [];` clear loop in stop(). Nothing populates scheduledNotes (verify with grep before deleting), so this is pure dead-code removal.
Keep GrooveEngine React-free. Explicit return types. No `any`.
  </action>
  <verify>
    <automated>npx vitest run src/core/GrooveEngine.test.ts</automated>
  </verify>
  <done>New P6 test passes (stop deferred to full duration). scheduledNotes field + clear loop gone (`grep -n scheduledNotes src/core/GrooveEngine.ts` returns nothing). Full suite green (`npx vitest run`). Commit: `fix: defer non-loop playback stop to full groove duration and drop dead scheduledNotes (#125)`</done>
</task>

<task type="auto">
  <name>Task 5 (Commit 5 — refactor: cleanup C2,C3,C4,C5,C6,C9,C10 + perf P4)</name>
  <files>src/hooks/useGrooveEngine.ts, src/components/production/ShareModal.tsx, src/midi/PerformanceTracker.ts, src/midi/MIDIHandler.ts, src/midi/KeyboardMIDISimulator.ts, src/midi/MIDIAccess.ts, src/hooks/useMIDIInput.ts, src/utils/safeStorage.ts, CLAUDE.md</files>
  <action>
C2 (useGrooveEngine.ts): Reimplement the six metronome setters (setMetronomeFrequency/Solo/CountIn/OffsetClick/Volume) as thin wrappers over the existing `setMetronomeConfig(partial)` useCallback. Each setter calls `setMetronomeConfig({ frequency })` etc. Preserve the volume clamp (0-100) and any frequency-specific engine call semantics — verify the engine-level method calls (`engine.setMetronomeFrequency` etc.) are still exercised via setMetronomeConfig's `engine.setMetronomeConfig(config)`; if setMetronomeConfig already applies the partial to the engine, the wrappers are correct. Keep exported API identical (same names, same signatures).
C3 (ShareModal.tsx): Extract one helper `copyToClipboard(text: string, kind: string): Promise<void>` that does `navigator.clipboard.writeText(text)` plus shared UI state. Keep the per-kind analytics tracking (`trackShareMethod('link'|'embed'|...)`) at the call sites (or pass the analytics method as the `kind` arg and call accordingly) — analytics behavior must be unchanged. Rewire handleCopyShortURL/handleCopyURL/handleCopyEmbed through the helper.
C4 (PerformanceTracker.ts + its test): Rename `PerformanceStats.timingErrors` → `timingScores` (the field stores accuracy scores, not ms). Update the interface, the initializers (`timingErrors: []` × resets), the push site (`this.stats.timingScores.push(timingAccuracy)`), the MAX trim shift, and getStats clone. Update the 3 references in PerformanceTracker.test.ts.
C5 (PerformanceTracker.ts): Remove the `[key: string]: any;` index signature from the GroovePattern interface (no-any convention). Fix any resulting type errors by adding explicit fields if needed.
C6 (MIDIHandler.ts): Replace the 3 `process.env.NODE_ENV === 'development'` checks with `import.meta.env.DEV`. (vitest sets import.meta.env.DEV true by default.)
C9 (KeyboardMIDISimulator.ts + MIDIAccess.ts + useMIDIInput.ts): Rebind hi-hat from Space (' ') to 'h' to avoid colliding with the Space=play/pause shortcut. Update DEFAULT_KEYBOARD_MIDI_CONFIG (`' '` → `'h'`), the file-header doc comment ("Space: Hi-hat" → "H: Hi-hat"), and the logged key-binding hint strings in MIDIAccess.ts (~"K=Kick, S=Snare, Space=Hi-hat" → "...H=Hi-hat") and useMIDIInput.ts (same). Grep for any other "Space=Hi-hat" / "Space: Hi-hat" mentions and update.
P4 (safeStorage.ts): Make the pre-write usage check cheap. Move the getStorageUsage() call OUT of the happy path of safeSetItem — only compute usage when the write FAILS (inside the catch, for the warning/error context) OR gate the pre-write check behind `logger.isDebugMode()`. CRITICAL: keep QuotaExceededError handling byte-for-byte identical (GrooveStorage tests depend on the returned `{ success:false, quotaExceeded:true, error }` shape). Choose the gate-behind-isDebugMode approach if it's the smaller diff and preserves the warning behavior in debug mode.
C10 (CLAUDE.md): Fix the stale dompurify claim on line ~64: change "dompurify - HTML sanitization for ShareModal and display" to reflect reality — dompurify is used in ExportUtils, not ShareModal (e.g. "dompurify - HTML sanitization in ExportUtils"). README/docs grep showed no dompurify mention, so no further doc edits.
Conventions throughout: explicit return types, null over undefined, no `any`, src/core+src/midi React-free.
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <done>Metronome setters delegate to setMetronomeConfig; ShareModal uses one clipboard helper with per-kind analytics intact; timingScores rename complete (`grep -rn "timingErrors" src/` returns nothing); GroovePattern has no `any`; MIDIHandler uses import.meta.env.DEV; hi-hat bound to 'h' everywhere (`grep -rni "Space.*Hi-hat" src/` returns nothing); safeSetItem doesn't iterate localStorage on every successful write; CLAUDE.md dompurify claim corrected. Full suite green. Commit: `refactor: dedupe setters/clipboard, rename timingScores, fix conventions, cheap storage write, rebind hi-hat (#125)`</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6 (Commit 6 — fix: C7 latency key migration + C8 codec alignment)</name>
  <files>src/utils/latencyStorage.ts, src/core/GrooveURLCodec.ts, src/core/GrooveURLCodec.test.ts</files>
  <behavior>
    - C7 test (new or extended latencyStorage test): on load, when the new kebab key is absent but the old `groovy_midi_latency_config` key is present, the loader migrates — reads old, writes new (`groovy-midi-latency-config`), removes old; subsequent load returns the same config. When only the new key exists, load works normally. When neither exists, returns null.
    - C8 test (extend GrooveURLCodec.test.ts): a pattern with an empty MIDDLE measure round-trips with correct per-measure alignment (no shift). Existing-format URLs (full-width measures, current encoders) still decode identically (backward compatibility regression).
  </behavior>
  <action>
C7 (latencyStorage.ts): Rename the storage key constant `LATENCY_CONFIG_KEY` from `'groovy_midi_latency_config'` to `'groovy-midi-latency-config'` (kebab-case convention). Add migration: define an `OLD_LATENCY_CONFIG_KEY = 'groovy_midi_latency_config'`. In the load path (and/or a one-time migrate helper called from load), if the new key is absent and the old key is present: read old value, write it to new key, remove the old key. Apply consistently so loadLatencyConfig / getAllLatencyConfigs see migrated data. Also replace the stray `console.error` in getAllLatencyConfigs with `logger.error` (consistency; logger already imported).
C8 (GrooveURLCodec.ts): Fix the two `.split(MEASURE_SEP).filter(p => p.length > 0)` sites (~line 297 and ~line 429) so empty middle measures keep positional alignment. Strip only LEADING/TRAILING separators, then `split(MEASURE_SEP)` WITHOUT filtering empties (so an empty middle measure yields an empty-string segment at the correct index). Decode each segment positionally by measure index. MUST remain backward-compatible: all current encoders emit full-width measures, so existing URLs (no empty segments) decode identically. Add regression tests for both existing-format URLs and the empty-middle-measure edge case.
Conventions: explicit return types, null over undefined, no `any`, src/core+src/utils React-free.
  </action>
  <verify>
    <automated>npx vitest run src/utils src/core/GrooveURLCodec.test.ts</automated>
  </verify>
  <done>New C7 migration test passes (old key → new key, data preserved). New C8 tests pass (empty-middle-measure aligned + existing-URL compat). `grep -n "groovy_midi_latency_config" src/utils/latencyStorage.ts` shows it only as the OLD-key migration constant. Full suite green (`npx vitest run`). Commit: `fix: migrate latency config key to kebab-case and fix codec measure alignment (#125)`</done>
</task>

</tasks>

<verification>
After all 6 commits:
- `npx vitest run` → 199/199 (or more, with added tests) green.
- `npm run build` → clean, abcjs in its own chunk, no >810kB warning on the main chunk.
- `git log --oneline` shows 6 commits, each `perf:/refactor:/fix:/docs:`-prefixed with `(#125)`, each independently green.
- Convention grep gates (filtered): no unconditional `console.` in the three hot-path files; no `timingErrors`; no `Space.*Hi-hat`; no `process.env.NODE_ENV` in MIDIHandler; no `[key: string]: any` in GroovePattern.
</verification>

<success_criteria>
- All 16 checklist items (P1-P6, C1-C10) implemented per the issue, with P3 via manualChunks (dynamic-import deferral noted in SUMMARY).
- Behavioral changes (P2, P4, P6, C4, C7, C8, C9) each covered by new/extended tests; pure deletions/renames (C1, C2, C3, C5, C6, C10, P1, P5) covered by the existing suite staying green.
- No regressions: latency config and URL codec remain backward-compatible; QuotaExceededError handling identical; useGrooveEngine exported API unchanged; 'midi-tracking-hit' event shape/values unchanged.
- Each commit self-contained and green.
</success_criteria>

<output>
After completion, create `.planning/quick/260611-g5v-performance-and-cleanup-improvements-fro/260611-g5v-SUMMARY.md` (note the P3 dynamic-import deferral rationale and final main-chunk size delta).
</output>
